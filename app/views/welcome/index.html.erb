<% if current_user.present? %>
  <%= react_component("DashboardApp", props: { projects: @projects }, prerender: false) %>
<% else %>
  <%= link_to "Sign In", new_user_session_path %>
<% end %>
<% Project.all.each do |p| %>
  <%= image_tag p.avatar(:thumb) %>
  <br>
<% end %>
